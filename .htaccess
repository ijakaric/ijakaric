Options +FollowSymLinks
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_URI} !dist/
    RewriteRule (.*) /dist/$1 [L]
    # http://localhost/example.com
    # RewriteRule (.*) /example.com/dist/$1 [L]
</IfModule>